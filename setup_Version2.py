from setuptools import setup, find_packages

setup(
    name="piepy",
    version="0.1.0",
    packages=["piepy"],
    install_requires=[],
    author="judedanger2012",
    description="pie.py is a math based game where you do problems to collect digits of pi for a baker.",
    python_requires=">=3.6",
)